# 二手苹果手机回收报价对比网站

收集各平台/渠道的二手 iPhone 回收报价，提供报价查询、多来源横向对比、到手价计算、报价提交、单条编辑/删除、CSV 导出与批量导入。

技术栈：HTML + TailwindCSS + JavaScript（前端） + Supabase（免费后端数据库），前端可免费部署到 Vercel。

## 文件清单

| 文件 | 作用 |
|---|---|
| `Supabase建表SQL.sql` | Supabase 建表语句（建表 + 行级安全策略），在 Supabase SQL 编辑器执行 |
| `index.html` | 网站主页：报价列表、筛选、到手价计算器、提交报价表单、导出 CSV、报价数据更新入口（单条编辑/删除/批量导入） |
| `compare.html` | 对比页：同机型同容量多来源横向对比，每档成色自动高亮最高价 |
| `quote_import.csv` | 报价批量导入模板，可用 Excel/WPS 编辑后导入 Supabase |
| `js/supabase.min.js` | Supabase SDK（本地引用，不依赖 CDN，避免浏览器跟踪防护拦截）；**部署时必须与 HTML 放在同一目录上传** |

## 部署步骤

### 1. 创建 Supabase 项目
1. 打开 https://supabase.com 注册并新建项目；
2. 左侧菜单 → SQL Editor，把 `Supabase建表SQL.sql` 全部内容粘贴进去执行；
   > 若此前执行过旧版 SQL（含管理员审核机制），重新执行本文件（末尾补丁语句会自动清理旧策略）即可切换为免登录模式；
3. 左侧菜单 → Project Settings → API，复制 `Project URL` 和 `anon public key` 两个值。

### 2. 导入历史报价（可选）
1. 用 Excel/WPS 打开 `quote_import.csv`，按模板把你的报价单整理进去；
2. 左侧菜单 → Table Editor → 打开 `iphone_quote` 表 → Import，选择 CSV 导入；
3. 导入完成后，把记录的 `status` 字段改为 `approved`，前端才会显示。

### 3. 配置已内置
`index.html`、`compare.html` 两处 JS 中的 `SUPABASE_URL` 与 `SUPABASE_ANON_KEY` 已替换为你的项目实际配置值，无需再修改。

> 说明：anon key（或新版 publishable key）是前端公开设计，配合 RLS 行级安全策略使用是安全的；**请勿把 Secret key（service_role）放进前端或发给任何人**。

### 4. 部署前端（免费）
1. 注册 https://vercel.com，导入一个 Git 仓库，或使用 vercel CLI 直接上传；
2. 把 `index.html`、`compare.html`、`quote_import.csv`、`js/` 整个目录一起上传（`js/supabase.min.js` 必须保留相对路径 `js/supabase.min.js`）；
3. 部署完成后访问：
   - 首页：`https://你的域名/index.html`
   - 对比页：`https://你的域名/compare.html`

## 功能说明

- **报价列表**：按机型、存储、来源筛选，展示 6 档成色价格（高保充新/靓机/小花/大花/外爆/内爆可测）及颜色差价、备注扣费；
- **到手价计算器**：选择机型+存储+成色+颜色差价+配件情况，自动计算预估到手价；
- **提交报价**：任何人可提交新报价，提交后直接公开（无需审核）；
- **报价数据更新**：点击首页"更新报价数据"按钮展开面板，可单条编辑已有报价（点击列表"编辑"按钮填充表单）、删除报价、或批量导入 CSV；
- **对比页**：同一机型+存储，横向并排展示各来源全部成色报价，最高价绿色高亮；
- **导出 CSV**：把当前筛选结果一键导出为 UTF-8 BOM 编码 CSV，Excel/WPS 打开不乱码，导出文件可再次导入。

## 数据库字段（iphone_quote 表）

| 字段 | 说明 |
|---|---|
| model | 机型，如 iPhone 17 PRO MAX |
| network_model | 网络型号，如 A3527 |
| storage | 容量，如 256G / 512G / 1T / 2T |
| price_high_keep_new | 高保充新价格 |
| price_good | 靓机价格 |
| price_small_scratch | 小花价格 |
| price_big_scratch | 大花价格 |
| price_out_break | 外爆价格 |
| price_inner_break | 内爆可测价格 |
| color_deduct | 颜色差价，如 蓝色-400 |
| note_deduct | 备注扣费，如 无全套需扣费 |
| source | 报价来源，如 手机联盟报价 / 爱回收 / 转转 |
| update_time | 报价更新时间 |
| status | approved 已公开（提交/导入时自动设置）；旧数据可能为 pending，需改为 approved 才会显示 |
| created_at | 创建时间 |

## 安全与合规注意事项

1. 当前方案取消管理员审核，任何人都可以直接提交、编辑、删除报价数据，建议后续在前端加一道简单口令验证防止恶意篡改；
2. **不要把 Secret key（service_role）放进前端代码或发给任何人**，前端只使用 anon key / publishable key；
3. 页面底部保留免责声明：报价仅供参考，实际回收价格以平台验机为准；
4. 回收报价每周变动，建议定期更新数据库中的报价数据。
